
<!-- show user AND user's items (index) -->

<!-- <%= @user.name %>. <%= link_to "Edit", item_path(@user) %> <%= link_to "Delete", item_path(@item), method: :delete %> -->

<!-- display user items -->
<!-- <%= image_tag @item[:photo_url] %>
<% @item.each do |item| %>
  <ul>
    <%= link_to "#{item.name}", item %>
  </ul>
<% end %> -->
